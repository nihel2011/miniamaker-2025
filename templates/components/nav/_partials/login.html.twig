<div class="d-flex align-items-center gap-3">
	{% if app.user %}
		<div class="dropdown">
			<a class="dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
				<img src="{{ asset('medias/images/default.png') }}" alt="{{ app.user.username ?? '' }}" class="rounded-circle" width="30">
				{{ app.user.username ?? '' }}
			</a>

			<ul class="dropdown-menu">
				{% if app.user %}
					<li>
						<a href="{{ path('app_logout') }}" class="text-decoration-none text-dark dropdown-item">
							Déconnexion
						</a>
					</li>
				{% else %}
					<li>
						<a href="{{ path('app_login') }}" class="text-decoration-none text-dark dropdown-item">
							Connexion
						</a>
					</li>
					<li>
						<a href="{{ path('app_register') }}" class="text-decoration-none text-dark dropdown-item">
							Inscription
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	{% else %}
		<a href="{{ path('app_login') }}" class="text-decoration-none text-dark">
			Connexion
		</a>
		<a href="{{ path('app_register') }}" class="btn btn-outline-primary text-decoration-none text-dark">
			Inscription
		</a>
	{% endif %}
</div>