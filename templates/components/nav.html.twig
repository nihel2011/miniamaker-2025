<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid justify-content-between">
        <div class="">
            {% include 'components/_partials/logo.html.twig' %}
        </div>
        <div class="" id="">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                            {{ ux_icon('mdi:home', { class: 'h-2'}) }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">
                            {{ ux_icon('mdi:account-circle', { class: 'h-2'}) }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                            {{ ux_icon('mdi:message-text', { class: 'h-2'}) }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            {{ ux_icon('mdi:logout', { class: 'h-2'}) }}
                        </a>
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            {{ ux_icon('mdi:login', { class: 'h-2'}) }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">
                            {{ ux_icon('mdi:account-circle', { class: 'h-2'}) }}
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>